type Comment {
  id: ID!
  text: String!
  user: User!
  post: Post!
  CommentlikesCount: Int!
  isCommentLiked: Boolean!
  createdAt: DateTime
  updatedAt: DateTime
}

type Query {
  comment(id: String): Comment
}
